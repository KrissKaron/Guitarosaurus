import matplotlib.pyplot as plt
import numpy as np
from keras import datasets, layers, models
import tensorflow as tf
import cv2 as cv

(training_imgs, training_labels), (testing_imgs, testing_labels) = datasets.
